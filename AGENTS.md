# 에이전트 작업 지침

저장소 전체에 적용하는 작업 규칙입니다. 최초 설치는 [README.md](README.md#시작)를 따릅니다.

## 명령

모든 명령은 저장소 루트에서 실행합니다.

| 명령                             | 용도                                 |
| -------------------------------- | ------------------------------------ |
| `mise run setup`                 | 의존성·스킬 설치                     |
| `mise run update`                | 개발 도구·의존성·외부 스킬 갱신      |
| `pnpm fix`                       | 린트 자동 수정·포맷                  |
| `pnpm check`                     | 포맷·린트·철자·타입·테스트 순차 검사 |
| `pnpm typecheck`                 | 루트와 패키지의 타입 검사            |
| `pnpm test`                      | 전체 Vitest 테스트                   |
| `pnpm test --project <패키지명>` | 패키지별 테스트                      |
| `pnpm exec vitest`               | 테스트 감시 모드                     |

명령 정의는 [package.json](package.json)과 [mise.toml](mise.toml), CI 실행 조건은 [CI 설정](.github/workflows/ci.yml)에서 확인합니다.

## 저장소 구조

| 경로                                 | 역할                                                   |
| ------------------------------------ | ------------------------------------------------------ |
| `apps/*`                             | 앱 패키지 위치                                         |
| `libs/*`                             | 공유 라이브러리 패키지 위치                            |
| `tools/*`                            | 개발 도구 패키지 위치                                  |
| `.github/`                           | GitHub Actions 등 저장소 자동화 설정                   |
| `.vscode/`                           | VS Code 작업 영역 설정·권장 확장                       |
| `.apm/skills/`                       | 로컬 스킬 원본                                         |
| `.agents/skills/`, `.claude/skills/` | 스킬 생성물                                            |
| 루트 설정 파일                       | 도구 버전·워크스페이스·타입 검사·린트·포맷·테스트 설정 |

패키지 검색 범위는 [pnpm-workspace.yaml](pnpm-workspace.yaml)을 따릅니다. 실행 환경·프레임워크·빌드·배포는 패키지별로 구성합니다.

도구 버전은 [mise.toml](mise.toml), 의존성은 각 `package.json`, 설치 버전은 잠금 파일에서 관리합니다. 문서에 버전을 중복 기록하지 않습니다.

## 작업 범위와 Git

변경 전에 요청 범위·관련 파일·검증 방법을 확인하고, 해당 패키지의 README와 기존 코드를 읽습니다. 여러 단계가 필요한 작업은 검증 가능한 단위로 나눕니다.

| 구분                       | 기준                                                                           |
| -------------------------- | ------------------------------------------------------------------------------ |
| 요청 범위에서 수행         | 현재 브랜치의 파일 수정, 관련 문서 갱신, 생성물 재생성, 검사 실행              |
| 사용자 요청이 있을 때 수행 | 커밋·푸시·풀 리퀘스트 생성                                                     |
| 수행하지 않음              | 사용자 변경 덮어쓰기, 비밀정보·로컬 환경 파일 커밋, 잠금 파일·생성물 직접 수정 |

## 코드·설정·문서

- 타입 검사는 [tsconfig.base.json](tsconfig.base.json), 린트는 [.oxlintrc.jsonc](.oxlintrc.jsonc), 포맷은 [.oxfmtrc.jsonc](.oxfmtrc.jsonc)와 [.editorconfig](.editorconfig)를 따릅니다.
- 코드의 명명·배치는 패키지의 기존 규칙을 따릅니다. 새로 정한 규칙은 해당 README에 기록합니다.
- 설정 기본값은 생략합니다. 필요한 주석은 목적·유지 이유·공식 참고 링크 순서로 작성합니다.
- 설정 주석 블록 위에 빈 줄을 둡니다. 파일 최상단·연속 주석 내부는 제외하고, 객체 시작 직후는 포맷 도구의 결과를 따릅니다.
- 작업 지침은 이 문서, 소개·최초 설치는 루트 README, 패키지별 사용법은 해당 README에 둡니다. 설정 근거는 주석에 작성하되, 주석을 지원하지 않는 파일은 해당 README에 남깁니다.
- 설명은 한 곳에서 관리하고 다른 문서에서는 링크로 연결합니다. 동작·절차 변경 시 관련 설명과 참조도 갱신합니다.

## 작업별 절차

### 패키지 추가

1. 용도에 맞는 디렉터리에 `package.json`과 `src/`를 추가합니다. ESM 패키지는 `"type": "module"`을 지정합니다.
2. `tsconfig.json`에서 공통 설정을 상속하고 실행 환경을 지정합니다. Node.js 패키지는 `module: nodenext`, `types: ["node"]`와 `@types/node` 개발 의존성을 추가합니다.
3. `"typecheck": "tsc"` 스크립트를 추가해 루트 타입 검사에 포함합니다. 테스트는 [테스트·검증](#테스트검증)에 따라 구성합니다.
4. `pnpm install`로 잠금 파일을 갱신합니다.

### 의존성 변경

- 공유 의존성은 워크스페이스 설정의 catalog에 선언하고 `catalog:`로 참조합니다.
- 내부 패키지는 `workspace:*`로 연결하고 진입점은 `exports`로 정의합니다.
- 선언을 변경한 뒤 `pnpm install`로 잠금 파일을 갱신합니다.

### 스킬 수정

로컬 원본을 수정한 뒤 `mise run setup`으로 배포합니다. 배포 대상과 외부 의존성은 [apm.yml](apm.yml)에서 관리합니다.

### 도구·의존성 갱신

- 일괄 갱신은 `mise run update` → `mise run setup`으로 실행합니다. 개발 도구·워크스페이스 의존성은 메이저 버전을 포함하고, 외부 스킬은 선언된 ref 범위에서 갱신합니다.
- `mise.toml`의 도구 버전을 직접 변경하면 `mise lock --platform macos-arm64,linux-x64`로 잠금 파일을 갱신합니다.
- 플랫폼을 추가하면 잠금 명령과 `mise.toml`의 `update` 플랫폼 목록에 함께 반영합니다.

## 테스트·검증

루트 [vitest.config.ts](vitest.config.ts)가 패키지별 테스트를 모아 실행합니다. 테스트가 있는 패키지에 다음 `vitest.config.ts`를 추가하고 실행 환경 등 프로젝트 옵션을 지정합니다.

```ts
import { defineProject } from "vitest/config";

export default defineProject({});
```

- 테스트 파일은 패키지 안에 두고 패키지 설정과 Vitest 검색 규칙에 맞춥니다. 동작 변경 시 필요한 테스트를 추가·수정합니다.
- `pnpm check`는 앞 단계가 실패하면 이후 검사를 실행하지 않습니다.
- 초기 테스트 제한은 [README.md](README.md#시작)를 참고합니다. 테스트를 추가하면 루트의 `passWithNoTests`를 제거합니다.

## 완료 조건

1. 요청한 변경과 관련 문서가 일치하는지 확인합니다.
2. `pnpm fix`로 정리합니다. 사용자 변경이 섞여 있으면 이번 작업의 파일만 개별 도구로 정리합니다.
3. 필요한 생성물을 재생성해 원본 변경과 함께 반영하고 `pnpm check`를 실행합니다. 이번 변경으로 발생한 실패는 해결합니다.
4. 변경 내용·검증 결과·남은 제약을 보고합니다. 기존 실패와 실행하지 못한 검사는 원인을 구분해 밝힙니다.
